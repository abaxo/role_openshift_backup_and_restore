---
# vars file for OpenshiftBackupRestore
namespace_export_location: "/opt/namespace-backups/{{ '%Y-%m-%d %H:%M:%S' | strftime(ansible_date_time.epoch) }}"
use_namespaces: all
export_exclude: 
  - openshift
  - openshift-infra
  - kube-system
  - logging
  - default
  - kube-public
  - management-infra
  - openshift-node
  - storage
